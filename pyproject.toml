[project]
dynamic = ["version", "license", "readme"]
name = "neurotools"
description = "Adds the searlight decoder."
# version = "0.3.3"
authors = [
    { name = "Spencer R. Loggia", email = "spncer.loggia98@gmail.com" }
]


[tool.setuptools]
packages = ["neurotools"]
